# Kafka Config
spring:
  kafka:
    bootstrap-servers: localhost:9092

    properties:
      schema.registry.url: http://localhost:8081
      enable.idempotence: false


INITIALS: csu-redpanda
topic-replicas: 1